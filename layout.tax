import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "Modibbo — Invest with clarity.",
  description:
    "A simulated stocks & crypto investment platform. Demo product — all market data and balances are simulated.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
